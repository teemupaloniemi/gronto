#lang racket

;; This is a dummy placeholder for the precomputed distances.
;; Should be only in this form in version control, not as the precomputed hash.
(provide precomputed)
(define precomputed #f)
